// Consider changing all dollar signs to jQuery to avoid conflict

// on home screen, want to initiate the fade-in once
// then set a cookie that prevents the fade-in from recurring

$(document).ready(function(){

  // check to see whether localStorage cookie has been set

  // if cookie has been set, don't start at black

  // if cookie has not been set, it must be the first visit, start at black

});